apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base
  - cloudfront-ingress.yaml

patches:
  - path: ingress-patch.yaml
  - path: nextcloud-patch.yaml
  - path: secret-patch.yaml